# Checkpoints

Trained checkpoints are not stored in git.

Expected paths for full reproduction:

```text
checkpoints/mcure171/session0.pth
checkpoints/flowers102/session0.pth
```

If public weights are released later, list each download URL, SHA-256 checksum,
and expected local path here.

